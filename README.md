# DAI-Server-Connector

## Vue环境安装

```shell
apt-get update
apt-get install sudo git curl
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
npm config set registry https://registry.npm.taobao.org/
npm install element-ui
npm install axios
npm install vue-router@3

# npm install -g @vue/cli --legacy-peer-deps
# npm install vue-router --legacy-peer-deps
# npm i webpack@5.82.0 --legacy-peer-deps
# npm i element-ui -S --legacy-peer-deps
# npm install --save axios --legacy-peer-deps
```